server:
   servlet:
      context-path: /movie-catalog-service-0.0.1-SNAPSHOT
spring:
  application:
    jmx:
      default-domain: movie-catalog-service
  cloud:
    config:
      override-system-properties: false
